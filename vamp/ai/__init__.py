"""AI layer (PLAN.md §9/§12 M5): a provider abstraction over the `claude`
CLI with a heuristic/template `none` fallback, so every AI feature keeps
working — degraded — with no provider available (CLAUDE.md hard rule)."""
