"""Strategy playbooks (PLAN.md §6): the seven written strategies, shipped as
markdown seed content and rendered in the UI. Each playbook pairs with the
pre-seeded prospects/scene-events that execute it (linked by ``playbook`` slug).
"""
