# Runtime dependencies — pure Python only (Termux/bionic has no manylinux
# wheels for compiled packages). See CLAUDE.md "Hard rules" and
# scripts/check_pure_python_deps.py, which enforces this in CI.
#
# Pinned via `pip freeze` against requirements.in on desktop Linux + CPython
# 3.11. Re-pin with: pip install -r requirements.in && pip freeze
anyio==4.14.2
APScheduler==3.11.3
beautifulsoup4==4.15.0
blinker==1.9.0
certifi==2026.6.17
click==8.4.2
feedparser==6.0.12
Flask==3.1.3
h11==0.16.0
httpcore==1.0.9
httpx==0.28.1
idna==3.18
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
PyYAML==6.0.1
sgmllib3k==1.0.0
soupsieve==2.9
typing_extensions==4.16.0
# tzdata: not imported directly anywhere in vamp/ — stdlib zoneinfo (used by
# tzlocal/APScheduler to resolve the phone's local timezone at startup)
# falls back to this package's bundled IANA database when the OS doesn't
# ship its own under /usr/share/zoneinfo. Termux doesn't, so without this
# pin the app crashes on Termux with "ZoneInfoNotFoundError: no time zone
# found with key America/..." even though it starts fine on desktop Linux
# (which almost always has system tzdata). Do not remove even if nothing
# `import`s it.
tzdata==2026.3
tzlocal==5.4.4
waitress==3.0.2
Werkzeug==3.1.8
