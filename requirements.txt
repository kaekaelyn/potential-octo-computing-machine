# Runtime dependencies — pure Python only (Termux/bionic has no manylinux
# wheels for compiled packages). See CLAUDE.md "Hard rules" and
# scripts/check_pure_python_deps.py, which enforces this in CI.
#
# Pinned via `pip freeze` against requirements.in on desktop Linux + CPython
# 3.11. Re-pin with: pip install -r requirements.in && pip freeze
blinker==1.9.0
click==8.4.2
Flask==3.1.3
itsdangerous==2.2.0
Jinja2==3.1.6
MarkupSafe==3.0.3
waitress==3.0.2
Werkzeug==3.1.8
